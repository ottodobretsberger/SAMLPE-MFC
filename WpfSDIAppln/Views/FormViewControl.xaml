<UserControl x:Class="WpfSDIAppln.Views.FormViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F6F9FC" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" FontSize="26" FontWeight="SemiBold" Foreground="#213146"/>
        <TextBlock Grid.Row="1" Text="{Binding Subtitle}" Margin="0,4,0,16" Foreground="#46576D"/>

        <Border Grid.Row="2" Background="White" BorderBrush="#DCE2EA" BorderThickness="1" Padding="12" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Exam Type" VerticalAlignment="Center" Margin="0,4,10,4"/>
                <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding ExamTypes}" SelectedItem="{Binding SelectedExamType}" Margin="0,2,0,2"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Patient Weight (kg)" VerticalAlignment="Center" Margin="0,4,10,4"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PatientWeightText, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,2"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Target Dose (mL/kg)" VerticalAlignment="Center" Margin="0,4,10,4"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding TargetDoseText, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,2"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Scanner Capacity (patients/hr)" VerticalAlignment="Center" Margin="0,4,10,4"/>
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding ScannerCapacityText, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,2"/>

                <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="Calculate" Width="100" Command="{Binding CalculateCommand}" Margin="0,0,10,0"/>
                    <Button Content="Reset" Width="100" Command="{Binding ResetCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="3" Background="White" BorderBrush="#DCE2EA" BorderThickness="1" Padding="12">
            <TextBox Text="{Binding ResultText}" IsReadOnly="True" BorderThickness="0" Background="Transparent"
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        </Border>
    </Grid>
</UserControl>
